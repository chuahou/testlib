path_classifiers:
  test:
    - exclude: / # please don't exclude my whole repo...
extraction:
  cpp:
    index:
      build_command:
        - mkdir build
        - cd build
        - cmake ..
        - make
